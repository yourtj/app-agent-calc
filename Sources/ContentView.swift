import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("Simple Calculator")
                .font(.title)
                .padding()
        }
    }
}

#Preview {
    ContentView()
}
